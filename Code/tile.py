#Lewis Mazzei - Fusion/Code/tile.py
#Note: If a comment is not on the same line then the corresponding comment to a line or chunk of code will be above it

class Tile():
	#constructor
	def __init__(self, element, location):
		self.element = element #set the 'element' attribute
		self.location = location #set the 'location' attribute
